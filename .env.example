# Optional: path to trained model checkpoint (.pt file)
# Leave empty to use random weights (useful for testing/development)
MODEL_PATH=

# Optional: path to saved tokenizer JSON
# Leave empty to use built-in default tokenizer
TOKENIZER_PATH=

# API server settings
LOG_LEVEL=info
PORT=8000
